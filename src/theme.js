import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#17605a",
            light: "skyblue"
        },
        secondary: {
            main: "#15c630"
        },
        otherColor: {
            main: "#999"
        }
    }
})